android {
            defaultConfig {
                applicationId = "org.nativescript.MyNativeScript2"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }